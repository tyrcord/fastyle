name: fastyle

packages:
  - packages/**

scripts:
  analyze:
    exec: dart analyze .
  import_sort:
    exec: dart pub run import_sorter:main
  dependency_validator:
    exec: dart run dependency_validator

ide:
  intellij:
    enabled: false
