name: fastyle_workspace

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  melos: ^6.1.0
  flutter_lints: ^4.0.0
  import_sorter: ^4.6.0
