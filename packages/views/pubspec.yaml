name: fastyle_views
description: Set of views Widgets for the fastyle library.
version: 0.0.6+1
repository: https://github.com/tyrcord/fastyle/tree/main/packages/views

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  fastyle_core: ^0.0.9
  tbloc: ^0.21.0
  fastyle_images: ^0.0.16+1
  easy_localization: ^3.0.2
  lingua_core: ^0.0.13
  go_router: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2
  dart_code_metrics: ^5.7.6
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.2
