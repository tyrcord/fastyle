name: fastyle_views
description: Set of views Widgets for the fastyle library.
version: 0.0.12
repository: https://github.com/tyrcord/fastyle/tree/main/packages/views

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

  # Public
  easy_localization: ^3.0.3
  go_router: ^10.2.0

  # Lingua
  lingua_core: ^0.0.26

  # T
  tbloc: ^0.21.4

  # Fastyle
  fastyle_core: ^0.0.21
  fastyle_images: ^0.0.24

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.2
