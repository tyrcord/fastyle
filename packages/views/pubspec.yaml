name: fastyle_views
description: Set of views Widgets for the fastyle library.
version: 0.0.9+3
repository: https://github.com/tyrcord/fastyle/tree/main/packages/views

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # Public
  easy_localization: ^3.0.3
  go_router: ^10.1.2

  # Lingua
  lingua_core: ^0.0.22

  # T
  tbloc: ^0.21.3+3

  # Fastyle
  fastyle_core: ^0.0.17
  fastyle_images: ^0.0.21+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.2
