name: fastyle_views
description: Set of views Widgets for the fastyle library.
version: 0.0.20
repository: https://github.com/tyrcord/fastyle/tree/main/packages/views

environment:
  sdk: ">=3.1.2 <4.0.0"
  flutter: ">=3.13.5"

dependencies:
  flutter:
    sdk: flutter

  # Public
  easy_localization: ^3.0.3
  go_router: ^12.1.0

  # Lingua
  lingua_core: ^0.0.31

  # T
  tbloc: ^0.21.7+5
  tenhance: ^0.0.3

  # Fastyle
  fastyle_core: ^0.0.32
  fastyle_images: ^0.0.34

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.3
