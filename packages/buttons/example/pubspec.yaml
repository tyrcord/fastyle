name: fastyle_buttons_example
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.2 <4.0.0"
  flutter: ">=3.13.5"

dependencies:
  flutter:
    sdk: flutter

  # Public
  easy_localization: ^3.0.3
  go_router: ^11.1.4

  # Lingua
  lingua_core: ^0.0.30

  # Fastyle
  fastyle_core: ^0.0.27
  fastyle_buttons: ^0.0.14+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.3

flutter:
  uses-material-design: true
