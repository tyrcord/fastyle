name: fastyle_buttons
description: Set of buttons Widgets for the fastyle library.
version: 0.0.14+1
repository: https://github.com/tyrcord/fastyle/tree/main/packages/buttons

environment:
  sdk: ">=3.1.2 <4.0.0"
  flutter: ">=3.13.5"

dependencies:
  flutter:
    sdk: flutter

  # Public
  font_awesome_flutter: ^10.6.0
  easy_localization: ^3.0.3
  go_router: ^11.1.4

  # Lingua
  lingua_core: ^0.0.30

  # Fastyle
  fastyle_core: ^0.0.26

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.3
