name: fastyle_calculator_example
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

  # Public
  go_router: ^12.1.1
  decimal: ^2.3.3

  # Fastyle
  fastyle_core: ^0.0.36+1
  fastyle_calculator: ^1.46.0

  # T
  tbloc: ^0.21.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.3

flutter:
  uses-material-design: true
