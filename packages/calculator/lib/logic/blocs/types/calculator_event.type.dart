enum FastCalculatorBlocEventType {
  clear,
  custom,
  compute,
  computed,
  computeFailed,
  init,
  initialized,
  initFailed,
  reset,
  patchValue,
  loadMetadata,
  patchMetadata,
  retrieveDefaultValues,
  saveEntryChanged,
  share,
  exportToPdf,
}
