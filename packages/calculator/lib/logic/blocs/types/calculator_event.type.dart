enum FastCalculatorBlocEventType {
  clear,
  custom,
  compute,
  computed,
  computeFailed,
  init,
  initialized,
  initFailed,
  reset,
  patchValue,
  resetValue,
  save,
  loadMetadata,
  patchMetadata,
  retrieveDefaultValues,
  saveEntryChanged,
}
