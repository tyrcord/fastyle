name: fastyle_iap
description: Set of iap Widgets for the fastyle library.
version: 0.0.12+1
repository: https://github.com/tyrcord/fastyle/tree/main/packages/iap

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # Public
  rxdart: ^0.27.7
  collection: ^1.17.2
  font_awesome_flutter: ^10.5.0
  easy_localization: ^3.0.3
  in_app_purchase_storekit: ^0.3.6+7
  in_app_purchase: ^3.1.10

  # Fastyle
  fastyle_core: ^0.0.19
  fastyle_buttons: ^0.0.9+1

  # Lingua
  lingua_core: ^0.0.23
  lingua_purchases: ^0.0.14

  # T
  tstore: ^0.8.15
  tbloc: ^0.21.4
  tmodel: ^0.5.8
  t_helpers: ^0.0.29

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.2
