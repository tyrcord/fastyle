name: fastyle_iap
description: Set of iap Widgets for the fastyle library.
version: 0.0.25+1
repository: https://github.com/tyrcord/fastyle/tree/main/packages/iap

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

  # Public
  rxdart: ^0.27.7
  collection: ^1.18.0
  font_awesome_flutter: ^10.6.0
  easy_localization: ^3.0.3
  in_app_purchase_storekit: ^0.3.6+7
  in_app_purchase: ^3.1.11

  # Fastyle
  fastyle_core: ^0.0.36+1
  fastyle_buttons: ^0.0.19+1

  # Lingua
  lingua_core: ^0.0.31
  lingua_purchases: ^0.0.17

  # T
  tstore: ^0.8.21
  tbloc: ^0.21.8
  tmodel: ^0.5.12
  t_helpers: ^0.0.42

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.3
