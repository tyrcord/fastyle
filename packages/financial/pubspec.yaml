name: fastyle_financial
description: Set of Financial Widgets for the fastyle library.
version: 0.0.1
homepage: https://github.com/tyrcord/fastyle/tree/main/packages/fastyle_financial

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  
  fastyle_dart: ">=1.2.4 <2.0.0"
  t_helpers: ">=0.0.16 <2.0.0"
  matex_dart: ^0.11.1
  collection: ^1.16.0
  fastyle_images: ^0.0.4+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  dart_code_metrics: ^5.7.5
  import_sorter: ^4.6.0
