name: fastyle_home
description: Set of home Widgets for the fastyle library.
version: 0.0.8
homepage: https://github.com/tyrcord/fastyle/tree/main/packages/home

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  fastyle_core: ^0.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2
  dart_code_metrics: ^5.7.6
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.2
