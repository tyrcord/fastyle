name: fastyle_forms_example
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.2 <4.0.0"
  flutter: ">=3.13.5"

dependencies:
  flutter:
    sdk: flutter

  # Public
  go_router: ^12.1.0

  # Fastyle
  fastyle_core: ^0.0.34
  fastyle_forms: ^0.0.32+1
  easy_localization: ^3.0.3

  # T
  tbloc: ^0.21.7+5

  # Lingua
  lingua_countries: ^0.0.4
  lingua_core: ^0.0.31

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.3

flutter:
  uses-material-design: true
