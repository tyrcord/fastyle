name: fastyle_forms_example
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.2 <4.0.0"
  flutter: ">=3.13.5"

dependencies:
  flutter:
    sdk: flutter

  # Fastyle
  fastyle_core: ^0.0.23
  fastyle_forms: ^0.0.23
  easy_localization: ^3.0.3

  # T
  tbloc: ^0.21.5

  # Lingua
  lingua_countries: ^0.0.3
  lingua_core: ^0.0.28

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.2

flutter:
  uses-material-design: true
