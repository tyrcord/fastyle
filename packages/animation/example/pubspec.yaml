name: fastyle_animation_example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Public
  go_router: ^11.1.4

  # Fastyle
  fastyle_core: ^0.0.25
  fastyle_animation: ^0.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.3

flutter:
  uses-material-design: true
