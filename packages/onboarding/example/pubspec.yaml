name: fastyle_onboarding_example
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.2 <4.0.0"
  flutter: ">=3.13.5"

dependencies:
  flutter:
    sdk: flutter

  # Public
  fastyle_core: ^0.0.33
  go_router: ^12.1.0
  font_awesome_flutter: ^10.6.0
  easy_localization: ^3.0.3

  # Lingua
  lingua_core: ^0.0.31
  lingua_onboarding: ^0.0.15
  lingua_countries: ^0.0.4

  # Fastyle
  fastyle_onboarding: ^0.0.34

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.3

flutter:
  uses-material-design: true
