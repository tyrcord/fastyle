name: fastyle_firebase
description: Set of firebase Widgets for the fastyle library.
version: 0.0.15
repository: https://github.com/tyrcord/fastyle/tree/main/packages/firebase

environment:
  sdk: ">=3.1.2 <4.0.0"
  flutter: ">=3.13.5"

dependencies:
  flutter:
    sdk: flutter

  # Public
  rxdart: ^0.27.7
  firebase_core: ^2.17.0
  firebase_analytics: ^10.5.1
  firebase_crashlytics: ^3.3.7
  firebase_remote_config: ^4.2.7
  firebase_performance: ^0.9.2+7
  firebase_messaging: ^14.6.9
  firebase_in_app_messaging: ^0.7.3+7
  firebase_app_check: ^0.2.0+1

  # Fastyle
  fastyle_core: ^0.0.25

  # T
  tbloc: ^0.21.5+2
  t_helpers: ^0.0.32

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.3
